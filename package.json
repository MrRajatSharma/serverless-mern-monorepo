{
  "name": "serverless-mern-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna bootstrap",
    "clean": "lerna clean",
    "clean-build": "npm run clean && npm run build"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
